/**
 * Jellyapp
 * This is the boilerplate for a Jelly model. The code is created with the
 *  Jelly CLI and can safely be edited
 */


// app/models/{MODELNAME_UNCAP}Model

const {
    baseModel
} = require('jellyapp')


module.exports = baseModel('{MODELNAME_CAP}', '{TABLENAME}', {
    // uuid: true
})
